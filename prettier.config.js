//  @ts-check

/** @type {import('prettier').Config} */
const config = {
	printWidth: 140,
	useTabs: true,
	semi: false,
	arrowParens: 'avoid',
	trailingComma: 'es5',
	singleQuote: true,
}

export default config
